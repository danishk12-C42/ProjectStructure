"""Security helpers: JWT creation/validation and password hashing.

Implement here when auth is built. Keeping all security-sensitive code in one
reviewed file (see CODEOWNERS) reduces the risk of scattered, inconsistent auth.
"""
# from datetime import datetime, timedelta, timezone
# from jose import jwt
# from passlib.context import CryptContext

# pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

# def create_access_token(...): ...
# def verify_password(...): ...
# def hash_password(...): ...
