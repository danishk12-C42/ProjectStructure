// Root component — routing lives here (or in a dedicated routes file as it grows).
export default function App() {
  return <h1>Project scaffold ready — start building features in src/features/</h1>;
}
