# src/types/

Global TypeScript types shared across the app (e.g. `ApiError`, pagination
envelope matching the backend's response shape).

Feature-specific types belong in `features/<x>/types/`, not here.
